# Plan: Feedback 7-Day Auto-Deletion

## Context
Feedbacks should auto-expire after 7 days. The codebase already has partial infrastructure:
- `seed.sql:137-142` — `delete_old_feedbacks()` SQL function exists, pg_cron scheduling is commented out
- `supabase-client.js:187-200` — `cleanOldFeedbacks()` JS method exists, only runs after submit
- `supabase-client.js:202-214` — `fetchFeedbacks()` fetches ALL feedbacks (no date filter)

## Files to Modify
1. **`js/supabase-client.js`** — Add date filtering to fetch + cleanup on both paths
2. **`seed.sql`** — Enable pg_cron scheduling for server-side auto-deletion

## Changes

### 1. `js/supabase-client.js` — `fetchFeedbacks()` (lines 202-214)
Add `.gte('created_at', cutoff)` to the Supabase query so only last 7 days are returned.
Also trigger `cleanOldFeedbacks()` on fetch as extra cleanup.
Filter localStorage fallback by date too.

### 2. `js/supabase-client.js` — `cleanOldFeedbacks()` (lines 187-200)
Add localStorage cleanup path (currently only works with Supabase).

### 3. `seed.sql` — pg_cron scheduling (line 144-145)
Uncomment and wrap pg_cron in a check so it doesn't fail if the extension isn't available.

## Verification
1. Submit a feedback on the public page — confirm it appears in admin
2. Verify only last-7-day feedbacks show in admin panel
3. Confirm pg_cron SQL runs without error in Supabase SQL Editor
