# Implementation Plan: Feedbacks, Content Management & Android Fix

## Context
The Persian cafe menu website already has all JS logic and Supabase CRUD for feedbacks and cafe_info, but the admin.html is missing the UI for these features. The public feedback form already works. Only 2 files need changes.

## Files to Modify
1. **`admin.html`** — Add sidebar nav items + 2 new page sections
2. **`css/main.css`** — Fix Android Chrome category chip scrolling

## No Changes Needed
- `js/admin.js` — All CRUD logic already exists (feedbacks: lines 24, 133, 172-196, 344-384; cafe info: lines 63-73, 387-420)
- `js/supabase-client.js` — All Supabase operations already exist
- `js/app.js` — Public feedback form already works
- `css/admin.css` — All needed styles already exist
- `index.html` — Feedback section already complete (lines 333-376)

---

## Step 1: admin.html — Add Sidebar Nav Items

**Location**: After line 80 (categories nav-item closing `</button>`), before line 81 (`</nav>`)

Add two new nav buttons:
- "انتقادات و پیشنهادات" → `activePage === 'feedbacks'`
- "مدیریت محتوا" → `activePage === 'content'` (calls `openEditCafeInfo()` on click to pre-populate form)

## Step 2: admin.html — Add Feedbacks Page

**Location**: After the categories page section (line 260), before `</main>` (line 262)

- Search input using existing `filteredFeedbacks` getter
- Table with columns: نام, پیام, تاریخ, عملیات
- Uses `formatDate()` for dates, `confirmDeleteFeedback()` for deletion
- Empty state when no feedbacks

## Step 3: admin.html — Add Content Management Page

**Location**: After the feedbacks page section

- Form fields for all cafeInfoForm properties: name, tagline, welcome_fa, about_fa, address_fa, phone, hours_fa, instagram, telegram
- Save button calls existing `saveCafeInfo()` method
- Uses existing `cafeInfoForm` Alpine model

## Step 4: admin.html — Add Feedback Count to Dashboard

**Location**: After the 4th stat card (visitCount, line 145), before the closing `</div>`

- Add 5th stat card showing `feedbackCount` (already computed in admin.js:172-174)

## Step 5: css/main.css — Fix Android Category Scrolling

**Location**: `.chip-container` rule (line 268-277)

Replace `overflow-x: auto` with `overflow-x: scroll` and add `-webkit-overflow-scrolling: touch`. This fixes Android Chrome not properly showing horizontal scroll on category chips.

## Verification
1. Open `index.html` — verify feedback form works, all sections present
2. Open `admin.html` — verify new sidebar items appear, feedbacks page shows data, content management form pre-populates and saves
3. Test on Android Chrome — verify category chips scroll smoothly
4. Toggle dark/light mode on both pages — verify consistency
