# Public Dark Mode, Favorites Filter, "محبوب" Rename, Circular Logo

## Changes

### 1. Public Dark Mode (`css/main.css` + `js/app.js` + `index.html`)

**CSS**: Add `.dark-mode` class with dark overrides:
```css
.dark-mode {
  --cafe-black: #f0ebe2;
  --cafe-bg: #1a1410;
  --cafe-card-bg: #241c14;
  --cafe-text: #e8e2d8;
  --cafe-text-muted: #a89880;
  --cafe-border: rgba(184, 134, 11, 0.15);
  --cafe-brown: #3d2e1f;
  --cafe-footer-bg: #2a2018;
}
```

**JS** (`app.js`): Add `darkMode` state, `loadTheme()`, `toggleTheme()`.

**HTML** (`index.html`):
- Add `:class="{ 'dark-mode': darkMode }"` on `<body>`
- Add toggle button in navbar (sun/moon icon next to favorites)

### 2. Favorites Filter (`js/app.js` + `index.html`)

The heart icon and localStorage system already work. Need to:
- Add `showFavoritesOnly` state to `app.js`
- Update `filteredProducts` getter to filter by favorites when active
- Make navbar heart icon clickable: `@click="showFavoritesOnly = !showFavoritesOnly"`
- Add a "علاقه‌مندی‌ها" chip in the category chips area (or toggle)
- Show favorites count in nav heart badge

### 3. "ویژه" → "محبوب" (all files)

Replace in:
- `index.html`: badge text, section titles, nav links, footer links
- `admin.html`: table headers, badge text, form labels, stat labels
- `js/data.js`: `is_featured` field name stays (internal), but UI text changes

### 4. Circular About Logo (`index.html` line 300)

Change inline style to include `border-radius:50%`, increase size to 100px.

---

## Files to Modify

| File | Changes |
|---|---|
| `css/main.css` | Add `.dark-mode` overrides |
| `js/app.js` | Add `darkMode`, `showFavoritesOnly`, update `filteredProducts` |
| `index.html` | Dark toggle in nav, favorites filter, "محبوب" rename, circular logo |
| `admin.html` | "محبوب" rename in UI text |
| `js/admin.js` | "محبوب" in stat label |

## Verification
1. Toggle dark mode on public page — all sections look good
2. Click heart in nav — filters to show only favorited products
3. All "ویژه" replaced with "محبوب"
4. About section logo is circular
